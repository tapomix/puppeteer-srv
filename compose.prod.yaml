services:
  puppeteer:
    # only expose the port for documentation
    build:
      target: prod
    expose:
      - ${SERVER_PORT}
    restart: unless-stopped
    deploy:
      restart_policy:
        condition: on-failure
        max_attempts: 2
